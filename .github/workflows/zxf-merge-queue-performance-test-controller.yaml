# SPDX-License-Identifier: Apache-2.0
name: "ZXF: [CITR] Merge Queue Performance Test Controller"
on:
  workflow_dispatch:
    inputs:
      test-asset:
        required: true
        default: "MQPT10"
        description: "Test on given MQ Machine (MQPT10-12)"
        type: choice
        options:
          - MQPT10
          - MQPT11
          - MQPT12
defaults:
  run:
    shell: bash

permissions:
  contents: write
  id-token: write
  actions: write
  statuses: write

jobs:
  extract-jdk-version:
    name: Extract Java Version
    uses: ./.github/workflows/802-extract-jdk-version.yaml
    with:
      ref: ${{ github.ref }}
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  extract-citr-vars:
    name: Extract CITR Vars
    uses: ./.github/workflows/855-extract-citr-vars.yaml
    with:
      ref: ${{ github.ref }}
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  run-merge-queue-performance-test:
    name: MQ Performance Test
    needs:
      - extract-jdk-version
      - extract-citr-vars
    uses: ./.github/workflows/zxc-merge-queue-performance-test.yaml
    with:
      test-asset: "${{ inputs.test-asset }}" # TODO: Once Chewie application is installed we can remove this input.
      ref: "${{ github.ref }}"
      solo-version: "${{ needs.extract-citr-vars.outputs.citr-solo-version }}"
      nlg-accounts: "20000000"
      nlg-time: "60"
      add-app-props: ""
      add-settings: ""
      run_NLGDparams: "-Dbenchmark.stepDuration=20m -Dbenchmark.coolDown=3m"
      delete-namespace: true
      java-version: "${{ needs.extract-jdk-version.outputs.jdk-version }}"
    secrets:
      slack-report-webhook: ${{ secrets.SLACK_CITR_DETAILED_REPORTS_WEBHOOK }}
      rootly-api-token: ${{ secrets.ROOTLY_API_TOKEN }}
      github-token: ${{ secrets.GITHUB_TOKEN }}

  get-commit-info:
    name: Get Commit Information
    runs-on: hl-cn-mqpt-lin-sm
    outputs:
      commit-author: ${{ steps.find-commit-author-slack.outputs.commit-author }}
      commit-email: ${{ steps.find-commit-author-slack.outputs.commit-email }}
      slack-user-id: ${{ steps.find-commit-author-slack.outputs.slack-user-id }}
      commit: ${{ steps.find-commit-author-slack.outputs.commit }}
    steps:
      - name: Prepare Runner
        id: prepare-runner
        uses: pandaswhocode/initialize-github-job@c392c3e699995ab6030915302d2fc9bffb1e861f # v1.1.0
        with:
          checkout: "true"
          checkout-ref: "${{ github.ref }}"
          checkout-token: "${{ secrets.GH_ACCESS_TOKEN }}"
          checkout-fetch-depth: "0"

      - name: Find Commit Author in Slack
        id: find-commit-author-slack
        continue-on-error: true
        env:
          BUILD_TAG: ${{ github.ref }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_CITR_BOT_TOKEN }}
        run: |
          # Extract the commit and user information from the build tag
          COMMIT=$(git rev-list -n 1 "${BUILD_TAG}")
          EMAIL=$(git log -1 --pretty=format:'%ae' "${COMMIT}")
          AUTHOR=$(git log -1 --pretty=format:'%an' "${COMMIT}")
          bash "${{ github.workspace }}/.github/workflows/support/scripts/find-commit-author-slack.sh"
          echo "commit=${COMMIT}" >> "${GITHUB_OUTPUT}"
          echo "slack-user-id=${SLACK_USER_ID}" >> "${GITHUB_OUTPUT}"
          echo "commit-author=${AUTHOR}" >> "${GITHUB_OUTPUT}"
          echo "commit-email=${EMAIL}" >> "${GITHUB_OUTPUT}"

  report-success:
    name: Report MQPT execution success
    runs-on: hl-cn-mqpt-lin-sm
    needs:
      - run-merge-queue-performance-test
      - get-commit-info
    if:
      ${{ (needs.run-merge-queue-performance-test.outputs.result == 'success' &&
      needs.get-commit-info.result == 'success') &&
      !cancelled() }}
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout Templates
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          sparse-checkout: |
            .github/workflows/templates
          sparse-checkout-cone-mode: false

      - name: Install gomplate
        run: |
          curl -sSfL "https://github.com/hairyhenderson/gomplate/releases/download/v5.0.0/gomplate_linux-amd64" \
            -o /tmp/gomplate
          curl -sSfL "https://github.com/hairyhenderson/gomplate/releases/download/v5.0.0/checksums-v5.0.0_sha256.txt" \
            -o /tmp/gomplate_checksums.txt
          echo "$(grep 'bin/gomplate_linux-amd64$' /tmp/gomplate_checksums.txt | awk '{print $1}')  /tmp/gomplate" | sha256sum -c -
          sudo install -m 755 /tmp/gomplate /usr/local/bin/gomplate

      - name: Build Slack Payload Message
        id: payload
        env:
          HEADER_TEXT: ":tada: MQPT - Merge Queue Performance Test (${{ github.ref }}) Passed"
          STATUS_TEXT: "*MQPT Job Succeeded. Test panel status below*"
          RUN_TEST_LABEL: "Run Merge Queue Performance Test"
          RUN_TEST_RESULT: ${{ needs.run-merge-queue-performance-test.outputs.result }}
          INFO_SECTION_TEXT: "*Workflow, Tag, and Commit Information*"
          SOURCE_TAG_URL: ${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ github.ref }}
          COMMIT_AUTHOR: ${{ needs.get-commit-info.outputs.commit-author }}
          WORKFLOW_RUN_ID: ${{ github.run_id }}
          WORKFLOW_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: gomplate -f .github/workflows/templates/slack-mqpt-success.json.tpl -o slack_payload.json

      - name: Report Success (slack)
        uses: slackapi/slack-github-action@b0fa283ad8fea605de13dc3f449259339835fc52 # v2.1.0
        with:
          webhook: ${{ secrets.SLACK_CITR_OPERATIONS_WEBHOOK }}
          webhook-type: incoming-webhook
          payload-templated: true
          payload-file-path: slack_payload.json

  report-failure:
    name: Report MQPT execution failure
    runs-on: hl-cn-mqpt-lin-sm
    needs:
      - run-merge-queue-performance-test
      - get-commit-info
      - report-success
    if:
      ${{ (needs.run-merge-queue-performance-test.outputs.result != 'success' ||
      needs.get-commit-info.result != 'success' ||
      needs.report-success.result != 'success') &&
      !cancelled() && always() }}

    steps:
      - name: Prepare Runner
        id: prepare-runner
        uses: pandaswhocode/initialize-github-job@c392c3e699995ab6030915302d2fc9bffb1e861f # v1.1.0
        with:
          checkout: "true"
          checkout-ref: "${{ github.ref }}"
          checkout-token: "${{ secrets.GH_ACCESS_TOKEN }}"
          checkout-fetch-depth: "0"
          setup-gomplate: "true"

      - name: Collect run logs in a log file
        env:
          GH_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
        continue-on-error: true
        run: |
          for job_id in $(gh run view ${{ github.run_id }} --json jobs --jq '.jobs | map(.databaseId) | .[0:-1] | .[]'); do
            echo "Fetching logs for job $job_id..."

            current_job_name=$(gh run view ${{ github.run_id }} --json jobs | jq --argjson job_id "$job_id" -r '.jobs[] | select(.databaseId == $job_id) | .name')

            echo "Logs for job $current_job_name :" >> run.log

            gh api \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            /repos/hiero-ledger/hiero-consensus-node/actions/jobs/$job_id/logs >> run.log
          done

      - name: Upload log as artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        continue-on-error: true
        with:
          path: run.log

      - name: Set Service Name
        id: set-service
        run: |
          ROOTLY_SERVICE="CITR SDLT"
          if [[ "${{ needs.get-commit-info.result }}" =~ ^(cancelled|failure)$ ]] \
            || [[ "${{ needs.report-success.result }}" =~ ^(cancelled|failure)$ ]]; then
            ROOTLY_SERVICE="CI/CD Workflows"
          fi
          echo "service=${ROOTLY_SERVICE}" >> "${GITHUB_OUTPUT}"

      - name: Build Rootly Summary
        id: rootly-summary
        run: |
          title="The MQPT Job Failed"
          echo "title=${title}" >> "${GITHUB_OUTPUT}"
          {
            echo 'summary<<EOF'
            echo "------------------------------------"
            echo "Status of each job:"
            echo "- Run Merge Queue Performance Test: ${{ needs.run-merge-queue-performance-test.outputs.result }}"
            echo "- Get Commit Info: ${{ needs.get-commit-info.result }}"
            echo "- Report Success: ${{ needs.report-success.result }}"
            echo "------------------------------------"
            echo "Commit information:"
            echo "- Commit: <${{ github.server_url }}/${{ github.repository }}/commit/${{ needs.get-commit-info.outputs.commit }}>"
            echo "- Workflow: <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}>"
            echo EOF
          } >> "${GITHUB_OUTPUT}"

      - name: Log Rootly Summary
        run: |
          echo "## Rootly Summary:"
          echo "### Title: ${{ steps.rootly-summary.outputs.title }}" >> "${GITHUB_STEP_SUMMARY}"
          echo "${{ steps.rootly-summary.outputs.summary }}" >> "${GITHUB_STEP_SUMMARY}"
          echo "### Service" >> "${GITHUB_STEP_SUMMARY}"
          echo "${{ steps.set-service.outputs.service }}" >> "${GITHUB_STEP_SUMMARY}"

      - name: Create Rootly Alert
        id: create-rootly-alert
        uses: pandaswhocode/rootly-alert-action@25cfdb8b2c006c1b130c183c7b1cbcffafb71f39 # v1.0.10
        continue-on-error: true # continue on error so we can get the incident reporting
        with:
          api_key: ${{ secrets.ROOTLY_API_TOKEN }}
          summary: ${{ steps.rootly-summary.outputs.title }}
          details: ${{ steps.rootly-summary.outputs.summary }}
          notification_target_type: "Service"
          notification_target: ${{ steps.set-service.outputs.service }}
          set_as_noise: "true"
          alert_urgency: "High"
          environments: "CITR"
          external_id: ${{ github.run_id }}
          external_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

      - name: Build Slack Payload Message
        id: payload
        env:
          HEADER_TEXT: ":grey_exclamation: MQPT - Merge Queue Performance Test (${{ github.ref }}) Failed"
          STATUS_TEXT: "*MQPT Job Resulted in failure. See status below.*"
          RUN_TEST_LABEL: "Run Merge Queue Performance Test"
          RUN_TEST_RESULT: ${{ needs.run-merge-queue-performance-test.outputs.result }}
          COMMIT_INFO_RESULT: ${{ needs.get-commit-info.result }}
          INFO_SECTION_TEXT: "*Workflow, Tag, and Commit Information*"
          SOURCE_TAG_URL: ${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ github.ref }}
          COMMIT_AUTHOR: ${{ needs.get-commit-info.outputs.commit-author }}
          SLACK_USER_ID: ${{ needs.get-commit-info.outputs.slack-user-id }}
          WORKFLOW_RUN_ID: ${{ github.run_id }}
          WORKFLOW_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: gomplate -f .github/workflows/templates/slack-mqpt-failure.json.tpl -o slack_payload.json

      - name: Report failure (slack citr-operations)
        uses: slackapi/slack-github-action@b0fa283ad8fea605de13dc3f449259339835fc52 # v2.1.0
        with:
          webhook: ${{ secrets.SLACK_CITR_OPERATIONS_WEBHOOK }}
          webhook-type: incoming-webhook
          payload-templated: true
          payload-file-path: slack_payload.json
